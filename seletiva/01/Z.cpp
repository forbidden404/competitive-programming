#include <bits/stdc++.h>

using namespace std;

int main()
{
	int a, b, x, y;
	while (scanf("%d %d %d %d", &a, &b, &x, &y) != EOF) {
		if (a == 0)
			break;
		int dx = x - a;
		int dy = y - a;

		if (a == x && b == y)
			printf("0\n");
		else if (

	}
	return 0;
}
